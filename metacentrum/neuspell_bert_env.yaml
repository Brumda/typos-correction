name: metacentrum_env
channels:
  - conda-forge
  - defaults
dependencies:
  - sentencepiece
  - pandas
  - pip
  - pip:
      - wandb
      - torch
      - torchaudio
      - torchvision
      - git+https://github.com/Brumda/neuspell-fixed